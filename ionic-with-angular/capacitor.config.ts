import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schwarzmuller.ionic-with-angular',
  appName: 'ionic-with-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
